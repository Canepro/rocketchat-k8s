# VEX Maintenance

The product purls in `rocketchat-vex.json` are pinned to the current Rocket.Chat image tag (`8.0.1`).
Update the purls whenever `values.yaml` changes the image tag so the VEX statements remain accurate.
