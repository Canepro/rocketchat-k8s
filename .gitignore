# Sensitive files - DO NOT COMMIT
grafana-cloud-secret.yaml
grafana-cloud-credentials.yaml
k8s-agent-values.yaml
*.key
*.pem

# SMTP credentials (if stored locally)
smtp-credentials.yaml

# Backup files
*.backup
*.bak
backups/
rocketchat-backup/

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db

# Temporary files
*.tmp
*.log

# Scripts (keep executable permissions)
# Note: On Windows, executable permissions are not tracked by git

# Helm charts (downloaded)
charts/
*.tgz

# Kubernetes secrets (if exported)
*-secret.yaml
*-secrets.yaml

# Exception: ArgoCD Application manifests may include "-secrets" in the filename but are not K8s Secret exports.
!GrafanaLocal/argocd/applications/*-secrets.yaml
!GrafanaLocal/argocd/applications/*external-secrets*.yaml


# Cursor (local)
.cursor/
.cursorignore
.cursorrules

# Terraform (local state and variable files may contain secrets)
terraform/.terraform/
terraform/.terraform.lock.hcl
terraform/*.tfstate
terraform/*.tfstate.*
terraform/terraform.tfvars
terraform/*.tfvars
terraform/backend.hcl